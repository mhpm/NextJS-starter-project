/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  images: {
    domains: ['images.unsplash.com', 'icons8.com', 'w3.org'],
  },
};

module.exports = nextConfig;
